using UnityEngine;

public class LancelotStoredStats : MonoBehaviour
{
    public static float currentEXP;
    public static int hpStat = 1, manaStat = 1,atkStat = 1 ,atkSpdStat = 1,spdStat = 1, cooldownStat = 1, playerLevel = 1, statPoints = 5;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
